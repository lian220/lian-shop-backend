# 사전 빌드된 JAR 파일을 사용하는 Dockerfile
# 사용법: 로컬에서 ./gradlew bootJar 실행 후 이 Dockerfile 사용
# 또는 GitHub Actions 등에서 빌드한 JAR 파일을 사용

FROM eclipse-temurin:21-jre-alpine
WORKDIR /app

# 빌드된 JAR 파일 복사 (로컬에서 빌드한 경우)
# 또는 GitHub Actions 등에서 다운로드한 JAR 파일 사용
COPY build/libs/*.jar app.jar

EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]

